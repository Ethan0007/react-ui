import styled from 'styled-components/native'
import {
  space,
  borders,
  width,
  height,
  size,
  color
} from 'styled-system'

const StyledImage = styled.Image(
  {},
  space,
  borders,
  width,
  height,
  size,
  color
)

export default StyledImage
